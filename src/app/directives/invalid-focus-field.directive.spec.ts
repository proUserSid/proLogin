import { InvalidFocusFieldDirective } from './invalid-focus-field.directive';

describe('InvalidFocusFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidFocusFieldDirective();
    expect(directive).toBeTruthy();
  });
});
